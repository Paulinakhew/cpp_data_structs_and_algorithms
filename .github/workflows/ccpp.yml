name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: make
      run: make cmake-build-debug/CMakeFiles/Makefile.cmake
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
